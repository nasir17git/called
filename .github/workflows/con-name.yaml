name: Reusable Workflow
run-name: aaa

concurrency:
  group: "aaaa"
  cancel-in-progress: false

on:
  workflow_call:
        
jobs:
  reusable_workflow_job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      # - run: sleep 30
      # - name: Dump GitHub context
      #   env:
      #     GITHUB_CONTEXT: ${{ toJson(github) }}
      #   run: |
      #     echo "$GITHUB_CONTEXT"
      - name: make_nested_variable
        run: |
          ggl=google
          url_done=https://www.${ggl}.com

      - name: make_msg
        run: |
          status=Succeeded
          url=https://www.${ggl}.com
          echo $url_done
          case $status in
            Failed|Stopped|Skipped)
              echo "msg=\\n${status} 정보: <$url_done|$ggl>" >> $GITHUB_ENV
              ;;
            Succeeded)
              echo "msg=\\n${status} 정보: <$url_done|$ggl>" >> $GITHUB_ENV
              ;;
          esac
        
      - name: Post to Slack channel
        id: slack
        uses: slackapi/slack-github-action@v1.24.0
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
        with:
          channel-id: 'C05J50BRRB3'  # Replace with your Slack channel ID or name
          payload: |
            {
              "text": "aa <${{env.url2}}|url2> , <${{env.url3}}|url3>,${{env.msg}}",
              "attachments": [
                {
                  "color": "#36a64f",
                  "blocks": [
                    {
                      "type": "section",
                      "text": {
                        "type": "mrkdwn",
                        "text": "aaa \n bbb "
                      }
                    },
                    {
                      "type": "section",
                      "text": {
                        "type": "mrkdwn",
                        "text": "bbb \n ccc"
                      }
                    }
                  ]
                }
              ]
            }